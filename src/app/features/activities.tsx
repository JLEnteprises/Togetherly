import { useCallback, useEffect, useMemo, useState } from 'react';
import { Alert, Pressable, View } from 'react-native';
import { router, useLocalSearchParams } from 'expo-router';
import { AppScreen } from '@/components/common/AppScreen';
import { BackHeader } from '@/components/common/BackHeader';
import { Card } from '@/components/common/Card';
import { AppText } from '@/components/common/AppText';
import { AppButton } from '@/components/common/AppButton';
import { FormField } from '@/components/common/FormField';
import { ChoiceChips } from '@/components/common/ChoiceChips';
import { TagChip } from '@/components/common/TagChip';
import { TagSelector } from '@/components/common/TagSelector';
import { ToggleRow } from '@/components/common/ToggleRow';
import { EmptyState } from '@/components/common/EmptyState';
import { ParticipantAttribution } from '@/components/common/ParticipantAttribution';
import { CollapsibleComposer } from '@/components/common/CollapsibleComposer';
import { ConfirmDialog } from '@/components/common/ConfirmDialog';
import { createActivity, deleteActivity, getActivities, getTags, setActivityInterest, updateActivity } from '@/services/backend/mvpFeatures';
import { useRealtimeRefresh } from '@/hooks/useRealtimeRefresh';
import { useWorkspace } from '@/providers/WorkspaceProvider';
import { useAppTheme } from '@/theme/useAppTheme';
import type { ActivityCost, ActivityEnvironment, ActivityLocationType, ActivityMood, ActivityStatus, ActivityTime, CoupleActivity, Tag } from '@/types/database';

function messageFrom(error: unknown) { return error instanceof Error ? error.message : 'Something went wrong.'; }
function durationLabel(minutes: number | null) { if (!minutes) return 'Flexible'; if (minutes < 60) return `${minutes} min`; const hours = minutes / 60; return `${Number.isInteger(hours) ? hours : hours.toFixed(1)} hr`; }
type Filter = 'all' | 'want_to_do' | 'planned' | 'favourite' | 'completed';

export default function ActivitiesScreen() {
  const theme = useAppTheme(); const params = useLocalSearchParams<{ focus?: string }>(); const { profile, partnerProfile, colorForUser } = useWorkspace();
  const [activities, setActivities] = useState<CoupleActivity[]>([]); const [tags, setTags] = useState<Tag[]>([]); const [title, setTitle] = useState(''); const [description, setDescription] = useState(''); const [cost, setCost] = useState<ActivityCost>('free'); const [locationType, setLocationType] = useState<ActivityLocationType>('anywhere'); const [environment, setEnvironment] = useState<ActivityEnvironment>('either'); const [mood, setMood] = useState<ActivityMood>('any'); const [timeOfDay, setTimeOfDay] = useState<ActivityTime>('any'); const [rating, setRating] = useState(''); const [duration, setDuration] = useState(''); const [location, setLocation] = useState(''); const [kidFriendly, setKidFriendly] = useState(false); const [booking, setBooking] = useState(false); const [selectedTags, setSelectedTags] = useState<string[]>([]);
  const [composerOpen, setComposerOpen] = useState(false); const [advancedOpen, setAdvancedOpen] = useState(false); const [filter, setFilter] = useState<Filter>('all'); const [busy, setBusy] = useState(false); const [editingId, setEditingId] = useState<string | null>(null); const [deleteTarget, setDeleteTarget] = useState<CoupleActivity | null>(null); const [loading, setLoading] = useState(true);

  const refresh = useCallback(async () => { try { const [nextActivities, nextTags] = await Promise.all([getActivities(), getTags()]); setActivities(nextActivities); setTags(nextTags); } catch (error) { Alert.alert('Couldn’t load activities', messageFrom(error)); } finally { setLoading(false); } }, []);
  useEffect(() => { refresh().catch(() => undefined); }, [refresh]); useRealtimeRefresh('activities', refresh); useRealtimeRefresh('tags', refresh);
  const visible = useMemo(() => activities.filter((activity) => filter === 'all' || activity.status === filter), [activities, filter]);
  function resetForm(close = true) { setEditingId(null); setTitle(''); setDescription(''); setCost('free'); setLocationType('anywhere'); setLocation(''); setEnvironment('either'); setMood('any'); setTimeOfDay('any'); setRating(''); setDuration(''); setKidFriendly(false); setBooking(false); setSelectedTags([]); setAdvancedOpen(false); if (close) setComposerOpen(false); }
  function beginEdit(activity: CoupleActivity) { setEditingId(activity.id); setTitle(activity.title); setDescription(activity.description ?? ''); setCost(activity.cost_level); setLocationType(activity.location_type); setLocation(activity.location ?? ''); setEnvironment(activity.environment); setMood(activity.mood); setTimeOfDay(activity.time_of_day); setRating(activity.rating == null ? '' : String(activity.rating)); setDuration(activity.duration_minutes == null ? '' : String(activity.duration_minutes)); setKidFriendly(Boolean(activity.kid_friendly)); setBooking(Boolean(activity.booking_required)); setSelectedTags((activity.tags ?? []).map((tag) => tag.id)); setAdvancedOpen(true); setComposerOpen(true); }
  useEffect(() => { if (!params.focus || editingId === params.focus || !activities.length) return; const focused = activities.find((activity) => activity.id === params.focus); if (focused) beginEdit(focused); }, [params.focus, activities]);
  async function save() { const durationMinutes = duration.trim() ? Number(duration) : null; if (!title.trim() || (durationMinutes != null && (!Number.isFinite(durationMinutes) || durationMinutes <= 0))) { Alert.alert('Check the activity', 'Add a title and a valid duration in minutes if you use one.'); return; } setBusy(true); try { const parsedRating = rating.trim() ? Number(rating) : null; if (parsedRating != null && (!Number.isInteger(parsedRating) || parsedRating < 1 || parsedRating > 5)) throw new Error('Rating must be a whole number from 1 to 5.'); const input = { title: title.trim(), description, costLevel: cost, locationType, location, environment, mood, timeOfDay, durationMinutes, kidFriendly, bookingRequired: booking, rating: parsedRating, tagIds: selectedTags }; if (editingId) await updateActivity(editingId, input); else await createActivity(input); resetForm(); await refresh(); } catch (error) { Alert.alert(editingId ? 'Couldn’t update activity' : 'Couldn’t add activity', messageFrom(error)); } finally { setBusy(false); } }
  async function toggleInterest(activity: CoupleActivity) { if (!profile) return; const current = activity.interests?.[profile.id] ?? false; try { await setActivityInterest(activity.id, !current); await refresh(); } catch (error) { Alert.alert('Couldn’t update interest', messageFrom(error)); } }
  async function setStatus(activity: CoupleActivity, status: ActivityStatus) { try { await updateActivity(activity.id, { status }); await refresh(); } catch (error) { Alert.alert('Couldn’t update activity', messageFrom(error)); } }
  async function removeConfirmed() { const target = deleteTarget; setDeleteTarget(null); if (!target) return; try { await deleteActivity(target.id); setActivities((current) => current.filter((activity) => activity.id !== target.id)); if (editingId === target.id) resetForm(); } catch (error) { Alert.alert('Couldn’t delete activity', messageFrom(error)); } }

  return <AppScreen>
    <BackHeader eyebrow="Together" title="Date ideas" subtitle="Save date ideas, then pick one together." />
    <View style={{ alignSelf: 'flex-start', marginBottom: theme.spacing.lg }}><AppButton compact label="✦ Open randomiser" onPress={() => router.push('/features/activity-randomizer' as never)} /></View>
    <CollapsibleComposer title={editingId ? 'Edit idea' : 'Saved ideas'} subtitle={`${activities.length} ideas in your library`} open={composerOpen} actionLabel="New idea" closeLabel={editingId ? 'Cancel edit' : 'Close'} tone="accent" style={{ marginBottom: theme.spacing.lg }} onToggle={() => composerOpen ? resetForm() : setComposerOpen(true)}>
      <FormField label="ACTIVITY" value={title} onChangeText={setTitle} placeholder="Stargazing" /><View style={{ gap: theme.spacing.sm }}><AppText variant="caption" tone="secondary">COST</AppText><ChoiceChips value={cost} onChange={setCost} options={[{ value: 'free', label: 'Free' }, { value: 'cheap', label: 'Cheap' }, { value: 'moderate', label: 'Moderate' }, { value: 'expensive', label: 'Expensive' }]} /></View><View style={{ gap: theme.spacing.sm }}><AppText variant="caption" tone="secondary">WHERE</AppText><ChoiceChips value={locationType} onChange={setLocationType} options={[{ value: 'home', label: 'Home' }, { value: 'nearby', label: 'Nearby' }, { value: 'online', label: 'Online' }, { value: 'anywhere', label: 'Anywhere' }]} /></View>
      <Pressable accessibilityRole="button" onPress={() => setAdvancedOpen((value) => !value)} style={{ flexDirection: 'row', justifyContent: 'space-between' }}><AppText variant="bodySmall" tone="accent">{advancedOpen ? 'Hide options' : 'More options'}</AppText><AppText tone="muted">{advancedOpen ? '⌃' : '⌄'}</AppText></Pressable>
      {advancedOpen ? <View style={{ gap: theme.spacing.lg }}><FormField label="DESCRIPTION" value={description} onChangeText={setDescription} multiline placeholder="What makes this a good date…" /><View style={{ gap: theme.spacing.sm }}><AppText variant="caption" tone="secondary">ENVIRONMENT</AppText><ChoiceChips value={environment} onChange={setEnvironment} options={[{ value: 'either', label: 'Either' }, { value: 'indoor', label: 'Indoor' }, { value: 'outdoor', label: 'Outdoor' }]} /></View><View style={{ gap: theme.spacing.sm }}><AppText variant="caption" tone="secondary">MOOD</AppText><ChoiceChips value={mood} onChange={setMood} options={[{ value: 'any', label: 'Anything' }, { value: 'romantic', label: 'Romantic' }, { value: 'relaxing', label: 'Relaxing' }, { value: 'adventurous', label: 'Adventure' }, { value: 'active', label: 'Active' }, { value: 'lazy', label: 'Lazy' }, { value: 'silly', label: 'Silly' }]} /></View><View style={{ gap: theme.spacing.sm }}><AppText variant="caption" tone="secondary">TIME OF DAY</AppText><ChoiceChips value={timeOfDay} onChange={setTimeOfDay} options={[{ value: 'any', label: 'Any' }, { value: 'morning', label: 'Morning' }, { value: 'day', label: 'Day' }, { value: 'night', label: 'Night' }]} /></View><View style={{ flexDirection: 'row', gap: theme.spacing.sm }}><View style={{ flex: 1 }}><FormField label="DURATION · MIN" value={duration} onChangeText={setDuration} keyboardType="number-pad" placeholder="120" /></View><View style={{ flex: 1 }}><FormField label="LOCATION · OPTIONAL" value={location} onChangeText={setLocation} placeholder="Lake, Discord…" /></View></View><FormField label="RATING · OPTIONAL 1–5" value={rating} onChangeText={setRating} keyboardType="number-pad" placeholder="5" /><ToggleRow label="Kid friendly" value={kidFriendly} onChange={setKidFriendly} /><ToggleRow label="Booking required" value={booking} onChange={setBooking} /><TagSelector tags={tags} selectedIds={selectedTags} onChange={setSelectedTags} /></View> : null}
      <AppButton label={busy ? 'Saving…' : editingId ? 'Save changes' : 'Add to our library'} disabled={busy || !title.trim()} onPress={save} />
    </CollapsibleComposer>
    <Card tone="secondary" style={{ marginBottom: theme.spacing.xxl, gap: theme.spacing.sm }}><AppText variant="caption" tone="secondary">FILTER</AppText><ChoiceChips value={filter} onChange={setFilter} options={[{ value: 'all', label: 'All' }, { value: 'want_to_do', label: 'Want to do' }, { value: 'planned', label: 'Planned' }, { value: 'favourite', label: 'Favourites' }, { value: 'completed', label: 'Done' }]} /></Card>
    <View style={{ gap: theme.spacing.md }}>{loading ? <AppText tone="muted">Loading activities…</AppText> : null}{!loading && visible.length === 0 ? <EmptyState icon="✦" title={activities.length ? "No ideas in this view" : "No date ideas yet"} body={activities.length ? "Switch filters to see other saved ideas." : "Save a few ideas for your next date."} actionLabel={activities.length ? undefined : "Add an idea"} onAction={activities.length ? undefined : () => setComposerOpen(true)} /> : null}{visible.map((activity) => { const myInterest = profile ? Boolean(activity.interests?.[profile.id]) : false; const partnerInterest = partnerProfile ? Boolean(activity.interests?.[partnerProfile.id]) : false; return <Card key={activity.id} participantColor={colorForUser(activity.creator_id)} style={{ gap: theme.spacing.md, opacity: activity.status === 'skip' ? 0.56 : 1, borderColor: editingId === activity.id ? theme.colors.accent : theme.colors.border }}><Pressable accessibilityRole="button" onPress={() => beginEdit(activity)} style={{ gap: theme.spacing.md }}><View style={{ flexDirection: 'row', justifyContent: 'space-between', gap: 12 }}><View style={{ flex: 1, gap: 5 }}><AppText variant="section">{activity.title}</AppText><ParticipantAttribution userId={activity.creator_id} />{activity.description ? <AppText variant="bodySmall" tone="secondary">{activity.description}</AppText> : null}</View><AppText tone="muted">›</AppText></View><View style={{ flexDirection: 'row', flexWrap: 'wrap', gap: 6 }}><TagChip subtle label={activity.cost_level.toUpperCase()} /><TagChip subtle label={activity.location_type.toUpperCase()} /><TagChip subtle label={activity.environment.toUpperCase()} /><TagChip subtle label={activity.time_of_day.toUpperCase()} /><TagChip subtle label={durationLabel(activity.duration_minutes).toUpperCase()} />{activity.kid_friendly ? <TagChip subtle label="KID FRIENDLY" /> : null}{activity.booking_required ? <TagChip subtle label="BOOKING REQUIRED" /> : null}{activity.rating ? <TagChip subtle label={`${'★'.repeat(activity.rating)} ${activity.rating}/5`} /> : null}{(activity.tags ?? []).map((tag) => <TagChip key={tag.id} subtle icon={tag.icon} iconDrawing={tag.icon_drawing} label={tag.name.toUpperCase()} />)}</View><AppText variant="caption" tone={myInterest && partnerInterest ? 'success' : 'secondary'}>{myInterest && partnerInterest ? '♥ BOTH INTERESTED' : myInterest ? `♥ ${profile!.display_name.toUpperCase()} IS INTERESTED` : partnerInterest ? `♥ ${partnerProfile!.display_name.toUpperCase()} IS INTERESTED` : 'NO INTEREST VOTES YET'}</AppText></Pressable><View style={{ flexDirection: 'row', flexWrap: 'wrap', gap: 8 }}><AppButton compact variant={myInterest ? 'secondary' : 'ghost'} label={myInterest ? '♥ Interested' : '♡ Interested'} onPress={() => toggleInterest(activity)} /><AppButton compact variant="ghost" label={activity.status === 'favourite' ? '★ Favourite' : '☆ Favourite'} onPress={() => setStatus(activity, activity.status === 'favourite' ? 'want_to_do' : 'favourite')} /><AppButton compact variant="secondary" label="Plan on calendar" onPress={() => router.push(`/features/calendar?prefillTitle=${encodeURIComponent(activity.title)}&prefillDescription=${encodeURIComponent(activity.description ?? '')}&prefillDuration=${activity.duration_minutes ?? 120}&sourceActivityId=${encodeURIComponent(activity.id)}` as never)} /><AppButton compact variant="ghost" label="Done" onPress={() => setStatus(activity, 'completed')} /><AppButton compact variant="ghost" label="Edit" onPress={() => beginEdit(activity)} /><AppButton compact variant="danger" label="Delete" onPress={() => setDeleteTarget(activity)} /></View></Card>; })}</View>
    <ConfirmDialog visible={!!deleteTarget} title="Delete activity?" body={deleteTarget ? `Delete “${deleteTarget.title}”?` : ''} onCancel={() => setDeleteTarget(null)} onConfirm={() => removeConfirmed().catch(() => undefined)} />
  </AppScreen>;
}
