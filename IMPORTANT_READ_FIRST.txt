TOGETHERLY v1.11 — POLISH & HARDENING

This overlay includes the complete v1.10 Live Location feature set plus v1.11 reliability, navigation, task-creation and production-copy improvements.

It intentionally does NOT include a root package.json, so copying it over the SDK57 and SDK54 projects will not replace their separate Expo dependency trees.

Latest database migration: 011_live_location.sql (no new v1.11 migration).
New v1.11 dependencies: none.

After applying the overlay, run the authoritative checks in APPLY_UPDATE_WINDOWS.md before considering the build fully validated on the live Windows/PostgreSQL setup.
