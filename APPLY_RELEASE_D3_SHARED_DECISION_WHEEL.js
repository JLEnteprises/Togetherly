const fs = require('fs');
const path = require('path');
const root = process.cwd();

function p(rel) { return path.join(root, rel); }
function read(rel) {
  const file = p(rel);
  if (!fs.existsSync(file)) throw new Error(`Missing ${rel}. Run this from the Togetherly project root.`);
  return fs.readFileSync(file, 'utf8').replace(/\r\n/g, '\n');
}
function write(rel, text) {
  const file = p(rel);
  fs.mkdirSync(path.dirname(file), { recursive: true });
  fs.writeFileSync(file, text.replace(/\n/g, '\r\n'), 'utf8');
  console.log(`Wrote ${rel}`);
}
function replaceOnce(op) {
  let text = read(op.rel);
  if (text.includes(op.new)) { console.log(`Already good ${op.rel}: ${op.label}`); return; }
  const i = text.indexOf(op.old);
  if (i < 0) throw new Error(`Could not safely patch ${op.rel}: ${op.label}`);
  if (text.indexOf(op.old, i + op.old.length) >= 0) throw new Error(`Multiple matches in ${op.rel}: ${op.label}`);
  write(op.rel, text.replace(op.old, op.new));
}
function insertBefore(op) {
  let text = read(op.rel);
  if (op.already && text.includes(op.already)) { console.log(`Already good ${op.rel}: ${op.label}`); return; }
  const i = text.indexOf(op.marker);
  if (i < 0) throw new Error(`Could not find insertion point in ${op.rel}: ${op.label}`);
  write(op.rel, text.slice(0, i) + op.addition + text.slice(i));
}

const baseline = [["src/app/features/decision-tools.tsx", "const [history, setHistory]"], ["server/migrations/016_emotional_rituals.sql", "daily_question_reveals"], ["src/app/features/timeline.tsx", "The moments that became your story."]];
for (const [rel, marker] of baseline) {
  if (!read(rel).includes(marker)) throw new Error(`${rel} is not the expected D2 baseline. Missing: ${marker}`);
}

write('server/migrations/017_shared_decision_wheel.sql', "-- Togetherly Release D3: shared server-authoritative decision wheel.\n\nCREATE TABLE IF NOT EXISTS decision_wheels (\n  couple_id uuid PRIMARY KEY REFERENCES couples(id) ON DELETE CASCADE,\n  options jsonb NOT NULL DEFAULT '[]'::jsonb,\n  winner text,\n  winner_index integer,\n  spin_id uuid,\n  spin_count integer NOT NULL DEFAULT 0,\n  updated_by uuid REFERENCES users(id) ON DELETE SET NULL,\n  spun_at timestamptz,\n  updated_at timestamptz NOT NULL DEFAULT now()\n);\n\nCREATE TABLE IF NOT EXISTS decision_wheel_spins (\n  id uuid PRIMARY KEY,\n  couple_id uuid NOT NULL REFERENCES couples(id) ON DELETE CASCADE,\n  spun_by uuid NOT NULL REFERENCES users(id) ON DELETE CASCADE,\n  options jsonb NOT NULL,\n  winner text NOT NULL,\n  winner_index integer NOT NULL,\n  created_at timestamptz NOT NULL DEFAULT now()\n);\n\nCREATE INDEX IF NOT EXISTS decision_wheel_spins_couple_idx\n  ON decision_wheel_spins(couple_id, created_at DESC);\n");

const ops = [{"kind": "insert_before", "rel": "src/types/database.ts", "marker": "export type DailyQuestion = {", "addition": "export type DecisionWheelSpin = {\n  id: string;\n  winner: string;\n  winner_index: number;\n  spun_by: string;\n  created_at: string;\n};\nexport type DecisionWheelState = {\n  options: string[];\n  winner: string | null;\n  winner_index: number | null;\n  spin_id: string | null;\n  spin_count: number;\n  updated_by: string | null;\n  spun_at: string | null;\n  updated_at: string | null;\n  history: DecisionWheelSpin[];\n};\n\n", "already": "export type DecisionWheelState =", "label": "decision wheel types"}, {"kind": "insert_before", "rel": "src/services/backend/mvpFeatures.ts", "marker": "export function getDailyQuestion()", "addition": "export function getDecisionWheel() { return apiRequest<DecisionWheelState>('/decision-wheel'); }\nexport function saveDecisionWheelOptions(options: string[]) { return apiRequest<DecisionWheelState>('/decision-wheel', { method: 'PUT', body: { options } }); }\nexport function spinDecisionWheel() { return apiRequest<DecisionWheelState>('/decision-wheel/spin', { method: 'POST' }); }\n\n", "already": "getDecisionWheel()", "label": "decision wheel API"}, {"kind": "replace", "rel": "src/services/backend/mvpFeatures.ts", "old": "  DrawingData,\n} from '@/types/database';", "new": "  DrawingData,\n  DecisionWheelState,\n} from '@/types/database';", "label": "decision wheel API type import"}, {"kind": "replace", "rel": "server/src/routes/together.ts", "old": "import { randomUUID } from 'node:crypto';", "new": "import { randomInt, randomUUID } from 'node:crypto';", "label": "secure decision-wheel random source"}, {"kind": "insert_before", "rel": "server/src/routes/together.ts", "marker": "async function loadTodaysQuestion", "addition": "function decisionWheelOptions(value: unknown) {\n  if (!Array.isArray(value)) throw new ApiError(400, 'Decision wheel options must be a list.');\n  const seen = new Set<string>();\n  const options: string[] = [];\n  for (const raw of value) {\n    if (typeof raw !== 'string') throw new ApiError(400, 'Each decision wheel option must be text.');\n    const option = raw.trim();\n    if (!option) continue;\n    if (option.length > 80) throw new ApiError(400, 'Decision wheel options must be 80 characters or fewer.');\n    const key = option.toLocaleLowerCase();\n    if (seen.has(key)) continue;\n    seen.add(key);\n    options.push(option);\n  }\n  if (options.length < 2 || options.length > 12) throw new ApiError(400, 'Choose between 2 and 12 unique decision wheel options.');\n  return options;\n}\n\nasync function loadDecisionWheelState(coupleId: string) {\n  const wheel = await pool.query(\n    `SELECT options,winner,winner_index,spin_id,spin_count,updated_by,spun_at,updated_at\n     FROM decision_wheels WHERE couple_id=$1`,\n    [coupleId],\n  );\n  const history = await pool.query(\n    `SELECT id,winner,winner_index,spun_by,created_at\n     FROM decision_wheel_spins WHERE couple_id=$1\n     ORDER BY created_at DESC LIMIT 5`,\n    [coupleId],\n  );\n  const row = wheel.rows[0];\n  return {\n    options: Array.isArray(row?.options) ? row.options : [],\n    winner: row?.winner ?? null,\n    winner_index: row?.winner_index == null ? null : Number(row.winner_index),\n    spin_id: row?.spin_id ?? null,\n    spin_count: Number(row?.spin_count ?? 0),\n    updated_by: row?.updated_by ?? null,\n    spun_at: row?.spun_at ?? null,\n    updated_at: row?.updated_at ?? null,\n    history: history.rows,\n  };\n}\n\n", "already": "function decisionWheelOptions", "label": "decision wheel helpers"}, {"kind": "insert_before", "rel": "server/src/routes/together.ts", "marker": "  app.get('/activities'", "addition": "  app.get('/decision-wheel', { preHandler: authenticate }, async (request, reply) => {\n    try {\n      const coupleId = await requireCoupleId(request.userId);\n      return reply.send(await loadDecisionWheelState(coupleId));\n    } catch (error) { return sendError(reply, error); }\n  });\n\n  app.put('/decision-wheel', { preHandler: authenticate }, async (request, reply) => {\n    try {\n      const coupleId = await requireCoupleId(request.userId);\n      const body = request.body as Record<string, unknown>;\n      const options = decisionWheelOptions(body.options);\n      await pool.query(\n        `INSERT INTO decision_wheels(couple_id,options,updated_by)\n         VALUES($1,$2::jsonb,$3)\n         ON CONFLICT(couple_id) DO UPDATE SET\n           options=EXCLUDED.options,\n           winner=NULL,\n           winner_index=NULL,\n           spin_id=NULL,\n           updated_by=EXCLUDED.updated_by,\n           spun_at=NULL,\n           updated_at=now()`,\n        [coupleId, JSON.stringify(options), request.userId],\n      );\n      broadcast(realtime, coupleId, 'decision_wheel', 'updated');\n      return reply.send(await loadDecisionWheelState(coupleId));\n    } catch (error) { return sendError(reply, error); }\n  });\n\n  app.post('/decision-wheel/spin', { preHandler: authenticate }, async (request, reply) => {\n    const client = await pool.connect();\n    try {\n      const coupleId = await requireCoupleId(request.userId);\n      await client.query('BEGIN');\n      const current = await client.query(\n        'SELECT options FROM decision_wheels WHERE couple_id=$1 FOR UPDATE',\n        [coupleId],\n      );\n      const options = decisionWheelOptions(current.rows[0]?.options ?? []);\n      const winnerIndex = randomInt(options.length);\n      const winner = options[winnerIndex];\n      const spinId = randomUUID();\n\n      await client.query(\n        `INSERT INTO decision_wheel_spins(id,couple_id,spun_by,options,winner,winner_index)\n         VALUES($1,$2,$3,$4::jsonb,$5,$6)`,\n        [spinId, coupleId, request.userId, JSON.stringify(options), winner, winnerIndex],\n      );\n      await client.query(\n        `UPDATE decision_wheels SET\n           winner=$1,\n           winner_index=$2,\n           spin_id=$3,\n           spin_count=spin_count+1,\n           updated_by=$4,\n           spun_at=now(),\n           updated_at=now()\n         WHERE couple_id=$5`,\n        [winner, winnerIndex, spinId, request.userId, coupleId],\n      );\n      await client.query('COMMIT');\n\n      broadcast(realtime, coupleId, 'decision_wheel', 'spun', spinId);\n      return reply.send(await loadDecisionWheelState(coupleId));\n    } catch (error) {\n      await client.query('ROLLBACK').catch(() => undefined);\n      return sendError(reply, error);\n    } finally {\n      client.release();\n    }\n  });\n\n", "already": "app.get('/decision-wheel'", "label": "shared decision wheel routes"}];
for (const op of ops) {
  if (op.kind === 'replace') replaceOnce(op);
  else if (op.kind === 'insert_before') insertBefore(op);
  else throw new Error(`Unknown operation: ${op.kind}`);
}

write('src/app/features/decision-tools.tsx', "import { useCallback, useEffect, useMemo, useRef, useState } from 'react';\nimport { Alert, Animated, Easing, View } from 'react-native';\nimport Svg, { Circle, Path, Text as SvgText } from 'react-native-svg';\nimport { AppScreen } from '@/components/common/AppScreen';\nimport { BackHeader } from '@/components/common/BackHeader';\nimport { Card } from '@/components/common/Card';\nimport { AppText } from '@/components/common/AppText';\nimport { AppButton } from '@/components/common/AppButton';\nimport { FormField } from '@/components/common/FormField';\nimport { ParticipantAttribution } from '@/components/common/ParticipantAttribution';\nimport { getDecisionWheel, saveDecisionWheelOptions, spinDecisionWheel } from '@/services/backend/mvpFeatures';\nimport { useRealtimeRefresh } from '@/hooks/useRealtimeRefresh';\nimport { useAppTheme } from '@/theme/useAppTheme';\nimport type { DecisionWheelState } from '@/types/database';\n\nfunction messageFrom(error: unknown) { return error instanceof Error ? error.message : 'Something went wrong.'; }\n\nfunction choicesFrom(value: string) {\n  const seen = new Set<string>();\n  return value\n    .split(/\\n|,/)\n    .map((item) => item.trim())\n    .filter((item) => {\n      if (!item) return false;\n      const key = item.toLocaleLowerCase();\n      if (seen.has(key)) return false;\n      seen.add(key);\n      return true;\n    })\n    .slice(0, 12);\n}\n\nfunction polar(size: number, radius: number, angleDegrees: number) {\n  const angle = (angleDegrees - 90) * Math.PI / 180;\n  const center = size / 2;\n  return {\n    x: center + radius * Math.cos(angle),\n    y: center + radius * Math.sin(angle),\n  };\n}\n\nfunction wedgePath(size: number, index: number, total: number) {\n  const center = size / 2;\n  const radius = size / 2 - 3;\n  const sweep = 360 / total;\n  const start = polar(size, radius, index * sweep);\n  const end = polar(size, radius, (index + 1) * sweep);\n  const largeArc = sweep > 180 ? 1 : 0;\n  return `M ${center} ${center} L ${start.x} ${start.y} A ${radius} ${radius} 0 ${largeArc} 1 ${end.x} ${end.y} Z`;\n}\n\nfunction WheelGraphic({ choices }: { choices: string[] }) {\n  const theme = useAppTheme();\n  const size = 286;\n  const palette = [theme.colors.accentSoft, theme.colors.secondarySoft, theme.colors.elevatedBackground];\n  const total = Math.max(choices.length, 1);\n\n  return (\n    <Svg width={size} height={size} viewBox={`0 0 ${size} ${size}`}>\n      {choices.map((choice, index) => {\n        const sweep = 360 / total;\n        const centerAngle = index * sweep + sweep / 2;\n        const label = polar(size, size * 0.31, centerAngle);\n        const short = choice.length > 15 ? `${choice.slice(0, 13)}…` : choice;\n        return (\n          <Path\n            key={`${choice}-${index}`}\n            d={wedgePath(size, index, total)}\n            fill={palette[index % palette.length]}\n            stroke={theme.colors.border}\n            strokeWidth={2}\n          />\n        );\n      })}\n      {choices.map((choice, index) => {\n        const sweep = 360 / total;\n        const centerAngle = index * sweep + sweep / 2;\n        const label = polar(size, size * 0.31, centerAngle);\n        const short = choice.length > 15 ? `${choice.slice(0, 13)}…` : choice;\n        return (\n          <SvgText\n            key={`label-${choice}-${index}`}\n            x={label.x}\n            y={label.y}\n            fill={theme.colors.textPrimary}\n            fontSize={10}\n            fontWeight=\"700\"\n            textAnchor=\"middle\"\n            alignmentBaseline=\"middle\"\n          >\n            {short}\n          </SvgText>\n        );\n      })}\n      <Circle cx={size / 2} cy={size / 2} r={25} fill={theme.colors.background} stroke={theme.colors.border} strokeWidth={2} />\n      <Circle cx={size / 2} cy={size / 2} r={8} fill={theme.colors.accent} />\n    </Svg>\n  );\n}\n\nfunction formatSpinTime(value: string) {\n  try {\n    return new Intl.DateTimeFormat(undefined, { hour: 'numeric', minute: '2-digit', month: 'short', day: 'numeric' }).format(new Date(value));\n  } catch {\n    return value;\n  }\n}\n\nexport default function DecisionToolsScreen() {\n  const theme = useAppTheme();\n  const [state, setState] = useState<DecisionWheelState | null>(null);\n  const [choicesText, setChoicesText] = useState('');\n  const [dirty, setDirty] = useState(false);\n  const [busy, setBusy] = useState(false);\n  const [loading, setLoading] = useState(true);\n\n  const rotation = useRef(new Animated.Value(0)).current;\n  const rotationDegrees = useRef(0);\n  const seenSpinId = useRef<string | null>(null);\n\n  const choices = useMemo(() => choicesFrom(choicesText), [choicesText]);\n\n  const refresh = useCallback(async () => {\n    try {\n      const next = await getDecisionWheel();\n      setState(next);\n      setChoicesText((current) => dirty ? current : next.options.join('\\n'));\n    } catch (error) {\n      Alert.alert('Couldn’t load the decision wheel', messageFrom(error));\n    } finally {\n      setLoading(false);\n    }\n  }, [dirty]);\n\n  useEffect(() => { refresh().catch(() => undefined); }, [refresh]);\n  useRealtimeRefresh('decision_wheel', refresh);\n\n  const animateToWinner = useCallback((winnerIndex: number, optionCount: number, immediate = false) => {\n    if (optionCount < 2) return;\n    const sweep = 360 / optionCount;\n    const desired = (360 - (winnerIndex * sweep + sweep / 2)) % 360;\n\n    if (immediate) {\n      rotation.stopAnimation();\n      rotation.setValue(desired);\n      rotationDegrees.current = desired;\n      return;\n    }\n\n    const currentNormalized = ((rotationDegrees.current % 360) + 360) % 360;\n    const forwardToDesired = (desired - currentNormalized + 360) % 360;\n    const target = rotationDegrees.current + 5 * 360 + forwardToDesired;\n    rotationDegrees.current = target;\n\n    Animated.timing(rotation, {\n      toValue: target,\n      duration: 2600,\n      easing: Easing.out(Easing.cubic),\n      useNativeDriver: true,\n    }).start(({ finished }) => {\n      if (!finished) return;\n      const normalized = ((target % 360) + 360) % 360;\n      rotation.setValue(normalized);\n      rotationDegrees.current = normalized;\n    });\n  }, [rotation]);\n\n  useEffect(() => {\n    if (!state?.spin_id || state.winner_index == null || state.options.length < 2) return;\n    if (seenSpinId.current === null) {\n      seenSpinId.current = state.spin_id;\n      animateToWinner(state.winner_index, state.options.length, true);\n      return;\n    }\n    if (seenSpinId.current === state.spin_id) return;\n    seenSpinId.current = state.spin_id;\n    animateToWinner(state.winner_index, state.options.length);\n  }, [animateToWinner, state?.options.length, state?.spin_id, state?.winner_index]);\n\n  async function saveOptionsOnly() {\n    if (choices.length < 2) return;\n    setBusy(true);\n    try {\n      const next = await saveDecisionWheelOptions(choices);\n      setState(next);\n      setChoicesText(next.options.join('\\n'));\n      setDirty(false);\n    } catch (error) {\n      Alert.alert('Couldn’t save wheel options', messageFrom(error));\n    } finally {\n      setBusy(false);\n    }\n  }\n\n  async function spin() {\n    if (choices.length < 2 || busy) return;\n    setBusy(true);\n    try {\n      let next = state;\n      const serverOptions = state?.options ?? [];\n      const changed = dirty || serverOptions.join('\\n') !== choices.join('\\n');\n      if (changed) {\n        next = await saveDecisionWheelOptions(choices);\n        setChoicesText(next.options.join('\\n'));\n        setDirty(false);\n      }\n      const spun = await spinDecisionWheel();\n      setState(spun);\n    } catch (error) {\n      Alert.alert('Couldn’t spin the wheel', messageFrom(error));\n    } finally {\n      setBusy(false);\n    }\n  }\n\n  const wheelChoices = state?.options?.length ? state.options : choices;\n  const rotate = rotation.interpolate({\n    inputRange: [0, 3600],\n    outputRange: ['0deg', '3600deg'],\n    extrapolate: 'extend',\n  });\n\n  return (\n    <AppScreen>\n      <BackHeader eyebrow=\"Together\" title=\"Decision wheel\" subtitle=\"A shared wheel. One spin, one answer, on both phones.\" />\n\n      <Card participantColor=\"both\" tone=\"accent\" style={{ gap: theme.spacing.lg, marginBottom: theme.spacing.xxl, alignItems: 'center', overflow: 'hidden' }}>\n        {loading ? <AppText tone=\"muted\">Loading shared wheel…</AppText> : null}\n\n        {wheelChoices.length >= 2 ? (\n          <View style={{ width: 300, height: 315, alignItems: 'center', justifyContent: 'flex-end' }}>\n            <View style={{\n              position: 'absolute',\n              top: 1,\n              zIndex: 4,\n              width: 0,\n              height: 0,\n              borderLeftWidth: 13,\n              borderRightWidth: 13,\n              borderTopWidth: 22,\n              borderLeftColor: 'transparent',\n              borderRightColor: 'transparent',\n              borderTopColor: theme.colors.textPrimary,\n            }} />\n            <Animated.View style={{ transform: [{ rotate }] }}>\n              <WheelGraphic choices={wheelChoices} />\n            </Animated.View>\n          </View>\n        ) : (\n          <View style={{ minHeight: 180, justifyContent: 'center', alignItems: 'center', paddingHorizontal: theme.spacing.xl }}>\n            <AppText variant=\"section\" align=\"center\">Add at least two options to build your wheel.</AppText>\n          </View>\n        )}\n\n        {state?.winner ? (\n          <View style={{ alignItems: 'center', gap: 4 }}>\n            <AppText variant=\"caption\" tone=\"secondary\">THE WHEEL LANDED ON</AppText>\n            <AppText variant=\"pageTitle\" align=\"center\">{state.winner}</AppText>\n            {state.spun_at ? <AppText variant=\"bodySmall\" tone=\"muted\">{formatSpinTime(state.spun_at)}</AppText> : null}\n          </View>\n        ) : null}\n\n        <AppButton\n          label={busy ? 'Spinning…' : dirty ? 'Save & spin' : state?.winner ? 'Spin again' : 'Spin together'}\n          disabled={busy || choices.length < 2}\n          onPress={spin}\n        />\n      </Card>\n\n      <Card tone=\"secondary\" style={{ gap: theme.spacing.lg, marginBottom: theme.spacing.xxl }}>\n        <View style={{ gap: 4 }}>\n          <AppText variant=\"section\">What are we choosing between?</AppText>\n          <AppText variant=\"bodySmall\" tone=\"secondary\">Two to twelve unique options. Saving updates the wheel for both of you.</AppText>\n        </View>\n        <FormField\n          label=\"OPTIONS · ONE PER LINE OR COMMA-SEPARATED\"\n          value={choicesText}\n          onChangeText={(value) => { setChoicesText(value); setDirty(true); }}\n          multiline\n          placeholder={'Takeaway\\nCook together\\nGo out'}\n        />\n        <View style={{ flexDirection: 'row', gap: theme.spacing.sm }}>\n          <View style={{ flex: 1 }}>\n            <AppButton variant=\"secondary\" label={busy ? 'Saving…' : 'Save shared options'} disabled={busy || choices.length < 2 || !dirty} onPress={saveOptionsOnly} />\n          </View>\n        </View>\n        <AppText variant=\"bodySmall\" tone={choices.length < 2 ? 'muted' : choices.length >= 12 ? 'warning' : 'secondary'}>\n          {choices.length}/12 options{choices.length >= 12 ? ' · extra entries are ignored' : ''}\n        </AppText>\n      </Card>\n\n      {state?.history?.length ? (\n        <Card style={{ gap: theme.spacing.md }}>\n          <View style={{ gap: 3 }}>\n            <AppText variant=\"section\">Recent shared spins</AppText>\n            <AppText variant=\"bodySmall\" tone=\"muted\">The same result is recorded for both of you.</AppText>\n          </View>\n          {state.history.map((item, index) => (\n            <View key={item.id} style={{ gap: 5, borderTopWidth: index ? 1 : 0, borderTopColor: theme.colors.border, paddingTop: index ? theme.spacing.sm : 0 }}>\n              <View style={{ flexDirection: 'row', justifyContent: 'space-between', gap: theme.spacing.md }}>\n                <AppText variant=\"cardTitle\" style={{ flex: 1 }}>{item.winner}</AppText>\n                <AppText variant=\"caption\" tone=\"muted\">{formatSpinTime(item.created_at)}</AppText>\n              </View>\n              <ParticipantAttribution userId={item.spun_by} />\n            </View>\n          ))}\n        </Card>\n      ) : null}\n    </AppScreen>\n  );\n}\n");

const checks = [["server/migrations/017_shared_decision_wheel.sql", "decision_wheel_spins"], ["server/src/routes/together.ts", "randomInt(options.length)"], ["server/src/routes/together.ts", "app.post('/decision-wheel/spin'"], ["server/src/routes/together.ts", "broadcast(realtime, coupleId, 'decision_wheel', 'spun'"], ["src/types/database.ts", "export type DecisionWheelState"], ["src/services/backend/mvpFeatures.ts", "spinDecisionWheel()"], ["src/app/features/decision-tools.tsx", "react-native-svg"], ["src/app/features/decision-tools.tsx", "useRealtimeRefresh('decision_wheel'"], ["src/app/features/decision-tools.tsx", "Animated.timing"], ["src/app/features/decision-tools.tsx", "Recent shared spins"]];
const missing = checks.filter(([rel, marker]) => !read(rel).includes(marker)).map(([rel, marker]) => `${rel}: missing ${marker}`);
if (missing.length) {
  fs.writeFileSync(p('RELEASE_D3_SHARED_WHEEL_AUDIT_REMAINING.txt'), missing.join('\r\n') + '\r\n', 'utf8');
  console.error(`D3 audit found ${missing.length} issue(s). See RELEASE_D3_SHARED_WHEEL_AUDIT_REMAINING.txt`);
  process.exitCode = 2;
} else {
  const report = p('RELEASE_D3_SHARED_WHEEL_AUDIT_REMAINING.txt');
  if (fs.existsSync(report)) fs.unlinkSync(report);
  console.log('D3 shared Decision Wheel audit clean.');
}

console.log('');
console.log('Release D3 applied.');
console.log('IMPORTANT: migration 017 is required.');
console.log('Run:');
console.log('  npm.cmd run backend:migrate');
console.log('  npm.cmd run typecheck');
console.log('  npm.cmd --prefix server run typecheck');
console.log('  npm.cmd --prefix server run logic');
