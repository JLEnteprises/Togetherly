const fs = require('fs');
const path = require('path');
const root = process.cwd();

function p(rel) { return path.join(root, rel); }
function read(rel) {
  const file = p(rel);
  if (!fs.existsSync(file)) throw new Error(`Missing ${rel}. Run this from the Togetherly project root.`);
  return fs.readFileSync(file, 'utf8').replace(/\r\n/g, '\n');
}
function write(rel, text) {
  const file = p(rel);
  fs.mkdirSync(path.dirname(file), { recursive: true });
  fs.writeFileSync(file, text.replace(/\n/g, '\r\n'), 'utf8');
  console.log(`Wrote ${rel}`);
}
function replaceOnce(op) {
  let text = read(op.rel);
  if (text.includes(op.new)) { console.log(`Already good ${op.rel}: ${op.label}`); return; }
  const i = text.indexOf(op.old);
  if (i < 0) throw new Error(`Could not safely patch ${op.rel}: ${op.label}`);
  if (text.indexOf(op.old, i + op.old.length) >= 0) throw new Error(`Multiple matches in ${op.rel}: ${op.label}`);
  write(op.rel, text.replace(op.old, op.new));
}

const baseline = [["server/migrations/016_emotional_rituals.sql", "daily_question_reveals"], ["src/app/features/memories.tsx", "MemoryDetailModal"], ["src/app/features/timeline.tsx", "Relationship timeline"]];
for (const [rel, marker] of baseline) {
  if (!read(rel).includes(marker)) throw new Error(`${rel} is not the expected D1 baseline. Missing: ${marker}`);
}

write('src/utils/memories.ts', "import type { CoupleMemory } from '@/types/database';\n\nexport function memoryPhotoUrls(memory: CoupleMemory | null | undefined) {\n  if (!memory) return [];\n  const photos = (memory.photos ?? []).map((photo) => photo.media_url).filter(Boolean);\n  if (photos.length) return photos;\n  return memory.photo_url ? [memory.photo_url] : [];\n}\n\nexport function memoryCoverUrl(memory: CoupleMemory | null | undefined) {\n  return memoryPhotoUrls(memory)[0] ?? null;\n}\n\nexport function memoryPhotoCount(memory: CoupleMemory | null | undefined) {\n  return memoryPhotoUrls(memory).length;\n}\n\nexport function formatMemoryDate(value: string, options: Intl.DateTimeFormatOptions = { month: 'long', day: 'numeric', year: 'numeric' }) {\n  try {\n    return new Intl.DateTimeFormat(undefined, options).format(new Date(`${value.slice(0, 10)}T12:00:00`));\n  } catch {\n    return value;\n  }\n}\n");
write('src/app/features/timeline.tsx', "import { useCallback, useEffect, useMemo, useState } from 'react';\nimport { Alert, Image, Pressable, View } from 'react-native';\nimport { router } from 'expo-router';\nimport { AppScreen } from '@/components/common/AppScreen';\nimport { BackHeader } from '@/components/common/BackHeader';\nimport { Card } from '@/components/common/Card';\nimport { AppText } from '@/components/common/AppText';\nimport { EmptyState } from '@/components/common/EmptyState';\nimport { ParticipantAttribution } from '@/components/common/ParticipantAttribution';\nimport { TagChip } from '@/components/common/TagChip';\nimport { MemoryDetailModal } from '@/components/memories/MemoryDetailModal';\nimport { getTimeline } from '@/services/backend/mvpFeatures';\nimport { useRealtimeRefresh } from '@/hooks/useRealtimeRefresh';\nimport { useWorkspace } from '@/providers/WorkspaceProvider';\nimport { participantPalette } from '@/theme/tokens';\nimport { useAppTheme } from '@/theme/useAppTheme';\nimport { formatMemoryDate, memoryCoverUrl, memoryPhotoCount } from '@/utils/memories';\nimport type { CoupleMemory } from '@/types/database';\n\nfunction messageFrom(error: unknown) { return error instanceof Error ? error.message : 'Something went wrong.'; }\n\nfunction syntheticRelationshipStart(date: string): CoupleMemory {\n  return {\n    id: 'relationship-start',\n    couple_id: '',\n    creator_id: '',\n    title: 'Relationship began',\n    description: 'The date saved in your couple profile.',\n    memory_date: date,\n    location: '',\n    is_milestone: true,\n    emoji: '♥',\n    photo_url: null,\n    photos: [],\n    tags: [],\n    created_at: date,\n    updated_at: date,\n  };\n}\n\nexport default function TimelineScreen() {\n  const theme = useAppTheme();\n  const { colorForUser } = useWorkspace();\n  const [milestones, setMilestones] = useState<CoupleMemory[]>([]);\n  const [relationshipStart, setRelationshipStart] = useState<string | null>(null);\n  const [detailTarget, setDetailTarget] = useState<CoupleMemory | null>(null);\n  const [loading, setLoading] = useState(true);\n\n  const refresh = useCallback(async () => {\n    try {\n      const result = await getTimeline();\n      setMilestones(result.milestones);\n      setRelationshipStart(result.couple?.relationship_start_date ?? null);\n    } catch (error) {\n      Alert.alert('Couldn’t load timeline', messageFrom(error));\n    } finally {\n      setLoading(false);\n    }\n  }, []);\n\n  useEffect(() => { refresh().catch(() => undefined); }, [refresh]);\n  useRealtimeRefresh('memories', refresh);\n\n  const timeline = useMemo(() => {\n    const items = [...milestones];\n    const hasRelationshipStart = relationshipStart && items.some((memory) =>\n      memory.memory_date === relationshipStart && memory.title.toLowerCase().includes('relationship'),\n    );\n    if (relationshipStart && !hasRelationshipStart) items.push(syntheticRelationshipStart(relationshipStart));\n    return items.sort((a, b) => a.memory_date.localeCompare(b.memory_date));\n  }, [milestones, relationshipStart]);\n\n  const chapters = useMemo(() => {\n    const groups = new Map<string, CoupleMemory[]>();\n    for (const memory of timeline) {\n      const year = memory.memory_date.slice(0, 4);\n      const group = groups.get(year) ?? [];\n      group.push(memory);\n      groups.set(year, group);\n    }\n    return [...groups.entries()];\n  }, [timeline]);\n\n  const photoMoments = milestones.filter((memory) => memoryPhotoCount(memory) > 0).length;\n\n  return (\n    <AppScreen>\n      <BackHeader eyebrow=\"Us\" title=\"Relationship timeline\" subtitle=\"The moments that became your story.\" />\n\n      {loading ? <AppText tone=\"muted\">Loading timeline…</AppText> : null}\n      {!loading && timeline.length === 0 ? (\n        <EmptyState\n          icon=\"timeline\"\n          title=\"No milestones yet\"\n          body=\"Mark a memory as a milestone and it becomes part of your relationship story.\"\n          actionLabel=\"Add a memory\"\n          onAction={() => router.push('/features/memories' as never)}\n        />\n      ) : null}\n\n      {timeline.length ? (\n        <View style={{ gap: theme.spacing.xxl }}>\n          <Card participantColor=\"both\" tone=\"accent\" style={{ gap: theme.spacing.sm, padding: theme.spacing.xl }}>\n            <AppText variant=\"caption\" tone=\"secondary\">OUR STORY</AppText>\n            <AppText variant=\"pageTitle\">{timeline.length} {timeline.length === 1 ? 'chapter' : 'chapters'} worth keeping.</AppText>\n            <AppText tone=\"secondary\">\n              {photoMoments\n                ? `${photoMoments} milestone ${photoMoments === 1 ? 'moment has' : 'moments have'} photos attached. Tap a saved memory to open the full story.`\n                : 'Add photos to milestone memories and they’ll become the visual anchors of your timeline.'}\n            </AppText>\n          </Card>\n\n          {chapters.map(([year, memories]) => (\n            <View key={year} style={{ gap: theme.spacing.md }}>\n              <View style={{ flexDirection: 'row', alignItems: 'center', gap: theme.spacing.md }}>\n                <AppText variant=\"section\">{year}</AppText>\n                <View style={{ flex: 1, height: 1, backgroundColor: theme.colors.border }} />\n                <AppText variant=\"caption\" tone=\"muted\">{memories.length} {memories.length === 1 ? 'moment' : 'moments'}</AppText>\n              </View>\n\n              <View>\n                {memories.map((memory, index) => {\n                  const synthetic = memory.id === 'relationship-start';\n                  const participantColor = memory.creator_id ? colorForUser(memory.creator_id) : 'both';\n                  const palette = participantColor === 'both' ? null : participantPalette(participantColor);\n                  const cover = memoryCoverUrl(memory);\n                  const photoCount = memoryPhotoCount(memory);\n\n                  return (\n                    <View key={memory.id} style={{ flexDirection: 'row', gap: theme.spacing.md }}>\n                      <View style={{ width: 34, alignItems: 'center' }}>\n                        <View style={{\n                          width: 16,\n                          height: 16,\n                          borderRadius: 8,\n                          marginTop: 18,\n                          backgroundColor: palette?.accent ?? theme.colors.secondaryAccent,\n                          borderWidth: 3,\n                          borderColor: theme.colors.background,\n                        }} />\n                        {index < memories.length - 1 ? <View style={{ flex: 1, width: 2, minHeight: 54, backgroundColor: theme.colors.vineSoft }} /> : null}\n                      </View>\n\n                      <Pressable\n                        accessibilityRole={synthetic ? undefined : 'button'}\n                        accessibilityLabel={synthetic ? undefined : `Open memory ${memory.title}`}\n                        disabled={synthetic}\n                        onPress={() => !synthetic && setDetailTarget(memory)}\n                        style={({ pressed }) => ({ flex: 1, opacity: pressed ? 0.8 : 1, marginBottom: theme.spacing.lg })}\n                      >\n                        <Card participantColor={participantColor} style={{ gap: theme.spacing.md, overflow: 'hidden', padding: 0 }}>\n                          {cover ? (\n                            <View>\n                              <Image\n                                source={{ uri: cover }}\n                                resizeMode=\"cover\"\n                                style={{ width: '100%', aspectRatio: 1.65, backgroundColor: theme.colors.elevatedBackground }}\n                              />\n                              {photoCount > 1 ? (\n                                <View style={{\n                                  position: 'absolute',\n                                  right: 10,\n                                  bottom: 10,\n                                  paddingHorizontal: 9,\n                                  paddingVertical: 5,\n                                  borderRadius: 999,\n                                  backgroundColor: 'rgba(0,0,0,0.64)',\n                                }}>\n                                  <AppText variant=\"caption\" style={{ color: '#ffffff' }}>{photoCount} PHOTOS</AppText>\n                                </View>\n                              ) : null}\n                            </View>\n                          ) : null}\n\n                          <View style={{ gap: theme.spacing.sm, padding: theme.spacing.lg }}>\n                            <View style={{ flexDirection: 'row', justifyContent: 'space-between', gap: theme.spacing.md, alignItems: 'flex-start' }}>\n                              <View style={{ flex: 1, gap: 4 }}>\n                                <AppText variant=\"caption\" tone=\"secondary\">\n                                  {formatMemoryDate(memory.memory_date, { month: 'short', day: 'numeric' }).toUpperCase()}\n                                </AppText>\n                                <View style={{ flexDirection: 'row', gap: 8, alignItems: 'center' }}>\n                                  <AppText variant=\"section\">{memory.emoji}</AppText>\n                                  <AppText variant=\"cardTitle\" style={{ flex: 1 }}>{memory.title}</AppText>\n                                </View>\n                              </View>\n                              <TagChip subtle label=\"MILESTONE\" />\n                            </View>\n\n                            {memory.creator_id ? <ParticipantAttribution userId={memory.creator_id} /> : null}\n                            {memory.location ? <AppText variant=\"bodySmall\" tone=\"muted\">{memory.location}</AppText> : null}\n                            {memory.description ? <AppText tone=\"secondary\" numberOfLines={cover ? 3 : 5}>{memory.description}</AppText> : null}\n                            {!synthetic ? <AppText variant=\"caption\" tone=\"accent\">TAP TO OPEN MEMORY</AppText> : null}\n                          </View>\n                        </Card>\n                      </Pressable>\n                    </View>\n                  );\n                })}\n              </View>\n            </View>\n          ))}\n        </View>\n      ) : null}\n\n      <MemoryDetailModal\n        memory={detailTarget}\n        visible={!!detailTarget}\n        onClose={() => setDetailTarget(null)}\n      />\n    </AppScreen>\n  );\n}\n");
write('src/components/memories/MemoryDetailModal.tsx', "import { useEffect, useMemo, useState } from 'react';\nimport { Image, Modal, Pressable, ScrollView, View } from 'react-native';\nimport { SafeAreaView } from 'react-native-safe-area-context';\nimport { AppButton } from '@/components/common/AppButton';\nimport { AppText } from '@/components/common/AppText';\nimport { Card } from '@/components/common/Card';\nimport { ParticipantAttribution } from '@/components/common/ParticipantAttribution';\nimport { TagChip } from '@/components/common/TagChip';\nimport type { CoupleMemory } from '@/types/database';\nimport { useAppTheme } from '@/theme/useAppTheme';\nimport { useWorkspace } from '@/providers/WorkspaceProvider';\nimport { formatMemoryDate, memoryPhotoUrls } from '@/utils/memories';\n\nexport function MemoryDetailModal({\n  memory,\n  visible,\n  initialPhotoIndex = 0,\n  openPhotoImmediately = false,\n  onClose,\n  onEdit,\n}: {\n  memory: CoupleMemory | null;\n  visible: boolean;\n  initialPhotoIndex?: number;\n  openPhotoImmediately?: boolean;\n  onClose: () => void;\n  onEdit?: (memory: CoupleMemory) => void;\n}) {\n  const theme = useAppTheme();\n  const { colorForUser } = useWorkspace();\n  const photos = useMemo(() => memoryPhotoUrls(memory), [memory]);\n  const [photoIndex, setPhotoIndex] = useState(0);\n  const [lightbox, setLightbox] = useState(false);\n\n  useEffect(() => {\n    if (!visible) return;\n    const safeIndex = Math.max(0, Math.min(photos.length - 1, initialPhotoIndex));\n    setPhotoIndex(Number.isFinite(safeIndex) ? safeIndex : 0);\n    setLightbox(openPhotoImmediately && photos.length > 0);\n  }, [initialPhotoIndex, openPhotoImmediately, photos.length, visible]);\n\n  if (!memory) return null;\n\n  return <>\n    <Modal visible={visible} animationType=\"slide\" presentationStyle=\"pageSheet\" onRequestClose={onClose}>\n      <SafeAreaView style={{ flex: 1, backgroundColor: theme.colors.background }} edges={['top', 'left', 'right', 'bottom']}>\n        <View style={{ paddingHorizontal: theme.spacing.xl, paddingTop: theme.spacing.md, paddingBottom: theme.spacing.sm, flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between', gap: theme.spacing.md }}>\n          <AppText variant=\"caption\" tone=\"secondary\">MEMORY</AppText>\n          <AppButton compact variant=\"ghost\" label=\"Close\" onPress={onClose} />\n        </View>\n\n        <ScrollView contentContainerStyle={{ paddingBottom: 50, gap: theme.spacing.xl }} showsVerticalScrollIndicator={false}>\n          {photos.length ? <View style={{ gap: theme.spacing.sm }}>\n            <Pressable accessibilityRole=\"button\" accessibilityLabel=\"Open photo full screen\" onPress={() => setLightbox(true)} style={({ pressed }) => ({ opacity: pressed ? 0.84 : 1 })}>\n              <Image source={{ uri: photos[photoIndex] }} resizeMode=\"cover\" style={{ width: '100%', aspectRatio: 1.15, backgroundColor: theme.colors.elevatedBackground }} />\n            </Pressable>\n            {photos.length > 1 ? <ScrollView horizontal showsHorizontalScrollIndicator={false} contentContainerStyle={{ gap: 8, paddingHorizontal: theme.spacing.xl }}>\n              {photos.map((url, index) => <Pressable key={`${url.slice(-30)}-${index}`} accessibilityRole=\"button\" accessibilityState={{ selected: index === photoIndex }} accessibilityLabel={`Photo ${index + 1} of ${photos.length}`} onPress={() => setPhotoIndex(index)}>\n                <Image source={{ uri: url }} style={{ width: 72, height: 72, borderRadius: theme.radii.sm, borderWidth: index === photoIndex ? 2 : 1, borderColor: index === photoIndex ? theme.colors.accent : theme.colors.border, backgroundColor: theme.colors.elevatedBackground }} resizeMode=\"cover\" />\n              </Pressable>)}\n            </ScrollView> : null}\n          </View> : null}\n\n          <View style={{ paddingHorizontal: theme.spacing.xl, gap: theme.spacing.lg }}>\n            <View style={{ gap: theme.spacing.sm }}>\n              <AppText variant=\"caption\" tone=\"secondary\">\n                {formatMemoryDate(memory.memory_date).toUpperCase()}\n              </AppText>\n              <View style={{ flexDirection: 'row', alignItems: 'flex-start', gap: theme.spacing.sm }}>\n                <AppText variant=\"pageTitle\">{memory.emoji}</AppText>\n                <AppText variant=\"pageTitle\" style={{ flex: 1 }}>{memory.title}</AppText>\n              </View>\n              <ParticipantAttribution userId={memory.creator_id} />\n              {memory.location ? <AppText variant=\"bodySmall\" tone=\"muted\">{memory.location}</AppText> : null}\n            </View>\n\n            {memory.description ? (\n              <Card participantColor={colorForUser(memory.creator_id)} tone=\"secondary\" style={{ gap: theme.spacing.sm, padding: theme.spacing.xl }}>\n                <AppText variant=\"caption\" tone=\"secondary\">THE STORY</AppText>\n                <AppText variant=\"section\" style={{ lineHeight: 28 }}>{memory.description}</AppText>\n              </Card>\n            ) : (\n              <Card tone=\"secondary\">\n                <AppText tone=\"muted\">No story text yet. The date and photos still keep the moment.</AppText>\n              </Card>\n            )}\n\n            <View style={{ flexDirection: 'row', flexWrap: 'wrap', gap: 6 }}>\n              {memory.is_milestone ? <TagChip label=\"MILESTONE\" /> : null}\n              {photos.length > 1 ? <TagChip subtle label={`${photos.length} PHOTOS`} /> : null}\n              {(memory.tags ?? []).map((tag) => <TagChip key={tag.id} subtle icon={tag.icon} iconDrawing={tag.icon_drawing} label={tag.name.toUpperCase()} />)}\n            </View>\n\n            {onEdit ? <AppButton variant=\"secondary\" label=\"Edit memory\" onPress={() => onEdit(memory)} /> : null}\n          </View>\n        </ScrollView>\n      </SafeAreaView>\n    </Modal>\n\n    <Modal visible={visible && lightbox} transparent animationType=\"fade\" onRequestClose={() => setLightbox(false)}>\n      <SafeAreaView style={{ flex: 1, backgroundColor: 'rgba(0,0,0,0.96)' }} edges={['top', 'left', 'right', 'bottom']}>\n        <View style={{ flex: 1, padding: theme.spacing.lg, gap: theme.spacing.md }}>\n          <View style={{ flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center' }}>\n            <AppText style={{ color: '#ffffff' }}>{photoIndex + 1} / {photos.length}</AppText>\n            <AppButton compact variant=\"ghost\" label=\"Close photo\" onPress={() => setLightbox(false)} />\n          </View>\n          <Pressable accessibilityRole=\"button\" accessibilityLabel=\"Close full screen photo\" onPress={() => setLightbox(false)} style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>\n            {photos[photoIndex] ? <Image source={{ uri: photos[photoIndex] }} resizeMode=\"contain\" style={{ width: '100%', height: '100%' }} /> : null}\n          </Pressable>\n          {photos.length > 1 ? <View style={{ flexDirection: 'row', gap: theme.spacing.sm }}>\n            <View style={{ flex: 1 }}><AppButton variant=\"secondary\" label=\"Previous\" disabled={photoIndex === 0} onPress={() => setPhotoIndex((current) => Math.max(0, current - 1))} /></View>\n            <View style={{ flex: 1 }}><AppButton variant=\"secondary\" label=\"Next\" disabled={photoIndex === photos.length - 1} onPress={() => setPhotoIndex((current) => Math.min(photos.length - 1, current + 1))} /></View>\n          </View> : null}\n        </View>\n      </SafeAreaView>\n    </Modal>\n  </>;\n}\n");

const ops = [{"kind": "replace", "rel": "src/app/features/memories.tsx", "old": "import { useAppTheme } from '@/theme/useAppTheme';\nimport type { CoupleMemory, Tag } from '@/types/database';", "new": "import { useAppTheme } from '@/theme/useAppTheme';\nimport { formatMemoryDate, memoryPhotoCount } from '@/utils/memories';\nimport type { CoupleMemory, Tag } from '@/types/database';", "label": "memory display utilities"}, {"kind": "replace", "rel": "src/app/features/memories.tsx", "old": "  const visible = useMemo(() => memories.filter((memory) => filter === 'all' || (filter === 'milestones' ? memory.is_milestone : filter === 'mine' ? memory.creator_id === profile?.id : memory.creator_id === partnerProfile?.id)), [filter, memories, partnerProfile?.id, profile?.id]);", "new": "  const visible = useMemo(() => memories.filter((memory) => filter === 'all' || (filter === 'milestones' ? memory.is_milestone : filter === 'mine' ? memory.creator_id === profile?.id : memory.creator_id === partnerProfile?.id)).sort((a, b) => b.memory_date.localeCompare(a.memory_date)), [filter, memories, partnerProfile?.id, profile?.id]);", "label": "newest memories first"}, {"kind": "replace", "rel": "src/app/features/memories.tsx", "old": "<BackHeader eyebrow=\"Us\" title=\"Memories\" subtitle=\"Keep the moment. Hide the admin.\" />", "new": "<BackHeader eyebrow=\"Us\" title=\"Memories\" subtitle=\"A photo-led record of the moments you want to keep.\" />", "label": "memory page story framing"}, {"kind": "replace", "rel": "src/app/features/memories.tsx", "old": "<AppText variant=\"caption\" tone=\"secondary\">{memory.memory_date}{memory.location ? ` · ${memory.location}` : ''}</AppText>", "new": "<AppText variant=\"caption\" tone=\"secondary\">{formatMemoryDate(memory.memory_date)}{memory.location ? ` · ${memory.location}` : ''}</AppText>", "label": "friendly memory dates"}, {"kind": "replace", "rel": "src/app/features/memories.tsx", "old": "<View style={{ flexDirection: 'row', flexWrap: 'wrap', gap: 6 }}>{memory.is_milestone ? <TagChip label=\"MILESTONE\" /> : null}{(memory.tags ?? []).slice(0, 3).map((tag) => <TagChip key={tag.id} subtle icon={tag.icon} iconDrawing={tag.icon_drawing} label={tag.name.toUpperCase()} />)}</View>", "new": "<View style={{ flexDirection: 'row', flexWrap: 'wrap', gap: 6 }}>{memory.is_milestone ? <TagChip label=\"MILESTONE\" /> : null}{memoryPhotoCount(memory) > 1 ? <TagChip subtle label={`${memoryPhotoCount(memory)} PHOTOS`} /> : null}{(memory.tags ?? []).slice(0, 3).map((tag) => <TagChip key={tag.id} subtle icon={tag.icon} iconDrawing={tag.icon_drawing} label={tag.name.toUpperCase()} />)}</View>", "label": "photo count chip"}];
for (const op of ops) replaceOnce(op);

const checks = [["src/utils/memories.ts", "memoryCoverUrl"], ["src/app/features/memories.tsx", "formatMemoryDate(memory.memory_date)"], ["src/app/features/memories.tsx", "memoryPhotoCount(memory) > 1"], ["src/app/features/timeline.tsx", "The moments that became your story."], ["src/app/features/timeline.tsx", "MemoryDetailModal"], ["src/app/features/timeline.tsx", "photoMoments"], ["src/components/memories/MemoryDetailModal.tsx", "THE STORY"], ["src/components/memories/MemoryDetailModal.tsx", "memoryPhotoUrls"]];
const missing = checks.filter(([rel, marker]) => !read(rel).includes(marker)).map(([rel, marker]) => `${rel}: missing ${marker}`);
if (missing.length) {
  fs.writeFileSync(p('RELEASE_D2_MEMORIES_TIMELINE_AUDIT_REMAINING.txt'), missing.join('\r\n') + '\r\n', 'utf8');
  console.error(`D2 audit found ${missing.length} issue(s). See RELEASE_D2_MEMORIES_TIMELINE_AUDIT_REMAINING.txt`);
  process.exitCode = 2;
} else {
  const report = p('RELEASE_D2_MEMORIES_TIMELINE_AUDIT_REMAINING.txt');
  if (fs.existsSync(report)) fs.unlinkSync(report);
  console.log('D2 Memories/Timeline audit clean.');
}

console.log('');
console.log('Release D2 applied.');
console.log('No migration is required.');
console.log('Run:');
console.log('  npm.cmd run typecheck');
console.log('  npm.cmd --prefix server run typecheck');
console.log('  npm.cmd --prefix server run logic');
